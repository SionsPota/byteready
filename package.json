{
  "name": "byteready",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ByteReady · TypeScript 全栈项目（Vite + React 前端 / Hono 后端）",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm --parallel --filter @byteready/web --filter @byteready/server run dev",
    "dev:web": "pnpm --filter @byteready/web run dev",
    "dev:server": "pnpm --filter @byteready/server run dev",
    "dev:lab": "pnpm --parallel --filter @byteready/web-lab --filter @byteready/server-lab run dev",
    "dev:web-lab": "pnpm --filter @byteready/web-lab run dev",
    "dev:server-lab": "pnpm --filter @byteready/server-lab run dev",
    "build": "pnpm -r run build",
    "test": "pnpm --filter @byteready/shared --filter @byteready/server run test",
    "typecheck": "pnpm -r run typecheck",
    "verify": "pnpm typecheck && pnpm test && pnpm build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "pnpm -r exec rm -rf dist build .turbo node_modules/.vite"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
